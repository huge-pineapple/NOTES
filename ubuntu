# GPU real-time usage
watch -n nvidia-smi 

# kill process
sudo kill -9 PID 

# global environment setting
~/.bashrc 
export PATH=.. :$PATH
source ~/.bashrc # global environment setting

# changing the version of CUDA 
sudo rm -rf cuda
sudo ln -s /usr/local/cuda-[version] /usr/local/cuda
